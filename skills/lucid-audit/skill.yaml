lucid: "1.0"
name: lucid-audit
version: "2.0.0"
description: "Smart contract security — AI brain layer with verdicts (SAFE/CAUTION/RISKY/CRITICAL), vulnerability detection, gas optimization, access control review, security scoring"
author: "Lucid"
license: "MIT"

requires:
  mcps:
    - "@modelcontextprotocol/sdk"
  env:
    - AUDIT_SUPABASE_URL
    - AUDIT_SUPABASE_KEY
    - AUDIT_ETHERSCAN_API_KEY

skills:
  - lucid_audit
  - lucid_audit_compare
  - lucid_audit_batch
  - lucid_gas
  - lucid_audit_pro

platforms:
  claude-code: true
  openclaw: true

entry:
  mcp: src/bin.ts
  openclaw: src/openclaw.ts
